import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.intel.platform',
  appName: 'Intelligence Platform',
  webDir: 'dist'
};

export default config;
